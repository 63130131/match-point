# Optional for local Node dev — copy to .env
JWT_SECRET=generate-a-long-random-string

# Admin can edit any player's name and photo (use your login username)
ADMIN_USERNAME=Miha

# cPanel: use php-api/config.php instead
